const PYTHON_LANGUAGE = 'Python';
const CSHARP_LANGUAGE = 'C#';
const JAVASCRIPT_LANGUAGE = 'Javascript';

const languageMapper = {
  py: PYTHON_LANGUAGE,
  cs: CSHARP_LANGUAGE,
  js: JAVASCRIPT_LANGUAGE,
};

export default languageMapper;
